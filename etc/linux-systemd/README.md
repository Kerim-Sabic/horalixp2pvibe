# Systemd Configuration

This directory contains configuration files for running Horalix P2P under the
"systemd" service manager on Linux both under either a systemd system service or
systemd user service. For further documentation take a look at the [systemd
section][1] on https://horalix.com/docs.

[1]: https://horalix.com/docs/users/autostart#using-systemd
